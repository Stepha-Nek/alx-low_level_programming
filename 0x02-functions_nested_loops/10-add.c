#include "main.h"
/**
 * add - adds two integers
 *
 * @x: first parameter to be added
 * @y: second parameter to be added
 * Return: Always 0 if successful
 */
int add(int x, int y)
{
	int z;

	z = x + y;
	return (z);

}

